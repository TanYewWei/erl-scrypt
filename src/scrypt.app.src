%% -*- mode: erlang; erlang-indent-level: 2; indent-tabs-mode: nil -*-
{application, scrypt,
 [
  {description, "Erlang wrapper around Colin Percival's SCrypt library"},
  {vsn, "1.1.1"},
  {registered, [scrypt_sup, scrypt_worker]},
  {applications, [ kernel,
                   stdlib,
                   crypto ]},
  {mod, {scrypt_app, []}},
  {env, []}
 ]}.
